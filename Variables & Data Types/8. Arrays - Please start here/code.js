// Arrays quick overview

var shoppingList = ['this','that','the other thing'];

console.log(shoppingList);
console.log(shoppingList[1]);// outputs second item - that 
console.log(shoppingList.length) // outputs the length of array

